model = "Qwen/Qwen3.5-4B"
algorithm = "opd"
thinking = false
seed = 42

# Optional experiment only. The supported Praxis path is SFT followed by GRPO;
# do not submit OPD until its teacher passes the same held-out evaluator.
[environment]
id = "bananagoo/praxis-freesolo"

[train]
epochs = 1
max_examples = 194
lora_rank = 16
